from data_utils import create_csv_with_points_from_patches

if __name__ == "__main__":
    create_csv_with_points_from_patches(parent_folder='/lustre/backup/SHARED/AIN/embed_interpret/data/',
                                        modalities=['tessera'])